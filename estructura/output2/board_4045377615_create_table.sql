CREATE TABLE `properties.monday_properties_equipment` (
  `name` STRING,
  `subelementos` STRING,
  `estado` STRING,
  `n_meros` FLOAT64,
  `texto_1` STRING,
  `texto72` STRING,
  `archivo_1` STRING,
  `estado1` STRING,
  `texto97` STRING,
  `archivo_16` STRING,
  `estado5` STRING,
  `texto91` STRING,
  `archivo_15` STRING,
  `estado15` STRING,
  `archivo84` STRING,
  `estado3` STRING,
  `archivo2` STRING,
  `estado8` STRING,
  `texto792` STRING,
  `archivo` STRING,
  `estado54` STRING,
  `texto29` STRING,
  `archivo5` STRING,
  `estado0` STRING,
  `archivo31` STRING,
  `estado17` STRING,
  `archivo96` STRING,
  `estado03` STRING,
  `archivo28` STRING,
  `estado2` STRING,
  `texto58` STRING,
  `archivo_14` STRING,
  `estado9` STRING,
  `texto0` STRING,
  `archivo27` STRING,
  `texto4` STRING,
  `archivo82` STRING,
  `text_mkqed5gj` STRING,
  `estado6` STRING,
  `texto79` STRING,
  `archivo7` STRING,
  `estado7` STRING,
  `texto75` STRING,
  `archivo51` STRING,
  `color` STRING,
  `archivo24` STRING,
  `estado39` STRING,
  `texto5` STRING,
  `archivo04` STRING,
  `estado31` STRING,
  `board_relation3` STRING,
  `pulse_id_mkqak00g` INT64,
  `pulse_log_mkqatmxw` TIMESTAMP,
  `pulse_updated_mkqae7xe` TIMESTAMP
);
