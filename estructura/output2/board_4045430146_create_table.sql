CREATE TABLE `properties.monday_properties_amenities` (
  `name` STRING,
  `estado_1` STRING,
  `texto_largo` STRING,
  `archivo` STRING,
  `color` STRING,
  `texto_largo7` STRING,
  `archivo5` STRING,
  `color0` STRING,
  `texto_largo2` STRING,
  `archivo8` STRING,
  `dup__of_gimnasio` STRING,
  `texto_largo0` STRING,
  `archivo0` STRING,
  `dup__of_alberca` STRING,
  `texto_largo8` STRING,
  `archivo9` STRING,
  `color4` STRING,
  `texto_largo24` STRING,
  `archivo50` STRING,
  `dup__of_jacuzzi` STRING,
  `texto_largo1` STRING,
  `archivo83` STRING,
  `dup__of_cine` STRING,
  `texto_largo5` STRING,
  `archivo04` STRING,
  `color02` STRING,
  `texto_largo94` STRING,
  `archivo69` STRING,
  `boolean7` BOOL,
  `dup__of_bodega` STRING,
  `texto_largo4` STRING,
  `archivo3` STRING,
  `dup__of_lavander_a` STRING,
  `texto` STRING,
  `texto_largo9` STRING,
  `archivo98` STRING,
  `estado` STRING,
  `board_relation9` STRING,
  `pulse_id_mkqaswrw` INT64,
  `pulse_log_mkqamgd4` TIMESTAMP,
  `pulse_updated_mkqaj0pe` TIMESTAMP
);
